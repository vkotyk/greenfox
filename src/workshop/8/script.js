/*
 * 8. FUNCTIONS
 *
 * EXCERCISE 1:
 * Create a method for drawing rectangles.
 * Think about the position of its corners/points.
 * How can you use variables in this case?
 *
 *
 * HELP:
 * function strokeSquare(left, top, size) {
    c.strokeRect(left, top, size, size);
  }
  strokeSquare(100,100,200);

  c.beginPath();
 * c.moveTo(100, 300);
 * c.lineTo(100,200);
 * c.fill();
 * c.stroke();
 */

/*
 * Your code goes into this function
 */
export function draw(c) {

}